<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <title>KSS Styleguide</title>
    <%= stylesheet_link_tag 'kss' %>
    <%= stylesheet_link_tag 'application' %>
    <%= stylesheet_link_tag 'rainbow/twilight' %>
    <style>
    .styleguide-example > h3{
      text-transform : none;
    }
    /* あんまり出していても意味が無いことも多い。*/
    .styleguide-element:not(.styleguide-modifier),
    .styleguide-element:not(.styleguide-modifier) + pre {
      display : none;
    }
    </style>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body class="kss-body">
    <header class="kss-header">
      KSS Styleguide Example
    </header>

    <div id="kss_wrapper">
      <nav role="main" class="kss-nav">
        <ul>
          <li><%= link_to 'Home', kss.root_path %></li>
          <% menu_list.each do |s| %>
          <li><%= link_to s, "/styleguide/#{s.downcase}" %></li>
          <% end %>
        </ul>
      </nav>

      <%= yield %>
    </div><!-- /#wrapper -->

    <%= javascript_include_tag 'kss' %>
    <%= javascript_include_tag 'rainbow' %>
  </body>
</html>
